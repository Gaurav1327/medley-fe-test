import React from 'react';

import AppRouter from './Routing';

const App: React.FC = () => {
    /* for any app initialisations */

    return (
        <React.Fragment>
            <AppRouter />
        </React.Fragment>
    );
};

export default App;
